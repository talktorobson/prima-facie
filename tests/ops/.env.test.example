# Example environment variables for operational testing
# Copy this file to .env.local in the project root and update with your values

# Supabase Configuration (Required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anonymous-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Environment Configuration
NODE_ENV=development

# Optional - For production testing
# VERCEL_URL=your-vercel-deployment-url
# NEXT_PUBLIC_VERCEL_URL=your-vercel-deployment-url

# Optional - Debug and logging
# DEBUG=true
# VERBOSE=true

# Notes:
# 1. Never commit actual keys to version control
# 2. Service role key should only be used in secure environments
# 3. Anonymous key can be public but should have proper RLS policies
# 4. Replace 'your-project-id' with your actual Supabase project ID